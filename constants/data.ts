import { NavItem } from '@/types';

export const navItems: NavItem[] = [
  {
    title: 'Payment',
    url: '/dashboard/payment',
    icon: 'user',
    isActive: false,
    items: [] // No child items
  }
];
